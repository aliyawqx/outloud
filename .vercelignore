node_modules
.next
.git

# Media / promo assets — not needed for the build, and huge (reel/ is multi-GB).
# NO blanket *.mp4 rule here: the landing demo clip public/demo/intro-v2.mp4 must
# ship with the deploy, and Vercel's ignore negation (!file) proved unreliable
# against a wildcard (the file 404'd in prod). Keep stray media inside these
# excluded directories instead.
reel
promo
reeldyn
cap_*.png
hi_*.png
site_*.png
